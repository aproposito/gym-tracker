{
  "name": "gym-tracker",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test tests/*.test.mjs",
    "test:deno": "deno test --allow-read tests/",
    "backtest": "deno run --allow-read tools/backtest-readiness.mjs"
  }
}
